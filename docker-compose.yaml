version: '3.8'
services:
  movies:
    container_name: backend
    build:
      context: ./movies
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
    stdin_open: true
    tty: true
    env_file:
      - .env